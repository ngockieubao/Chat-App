package com.tomosia.chatapp.util

import android.content.Context
import android.util.Patterns

object TextUtils {
//    fun isValidEmailPasswd(context: Context, input: String) {
//        if (input.isNullOrEmpty() && Patterns.EMAIL_ADDRESS.matcher(context.toString()).matches())
//            return input
//    }
}